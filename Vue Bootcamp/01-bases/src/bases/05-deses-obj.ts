// Desestructuración de objetos

const persona = {
    nombre: 'Tony',
    edad: 45,
    clave: 'Ironman'
};

// Extraer propiedades
const { nombre, edad, clave } = persona;

console.log(nombre);
console.log(edad);
console.log(clave);