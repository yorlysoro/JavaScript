// Constante
const producto = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

console.log(producto);
console.log(precio);
console.log(disponible);

// No se puede reasignar una constante
producto = "Monitor 24 pulgadas"; // Esto generará un error
console.log(producto);