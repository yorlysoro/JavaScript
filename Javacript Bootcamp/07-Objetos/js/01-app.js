const nombreProducto = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

// Agrupando en un objeto
const producto = {
    nombre: nombreProducto,
    precio: precio,
    disponible: disponible
};

console.log(producto); // { nombre: 'Monitor 20 pulgadas', precio: 300, disponible: true }